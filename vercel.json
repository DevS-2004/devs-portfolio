{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "exit 1", // Optional: prevent auto builds unless you want
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
